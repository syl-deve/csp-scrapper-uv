# CSP Scrapper 프로젝트 아키텍처

**작성일**: 2025-11-25  
**프로젝트**: csp-scrapper-uv  
**목적**: Cloud Service Provider 업데이트 자동 수집 및 뉴스레터 발송 시스템

---

## 📁 1. 프로젝트 파일 구조

```
csp-scrapper-uv/
├── .git/                          # Git 버전 관리
├── app/                           # 애플리케이션 소스 디렉토리
│   ├── main.py                    # 메인 실행 파일 (433줄)
│   ├── requirements.txt           # Python 의존성 정의
│   ├── banner.png                 # 이메일 배너 이미지 (738KB)
│   ├── .env.example               # 환경변수 템플릿
│   ├── .gitignore                 # Git 제외 파일 설정
│   └── output/                    # JSON 출력 디렉토리 (동적 생성)
├── Dockerfile                     # Docker 이미지 빌드 정의
├── README.md                      # 프로젝트 문서
└── docker-install.md              # Docker 설치 가이드
```

---

## 🏗️ 2. 시스템 아키텍처

### 2.1 전체 시스템 흐름도

```mermaid
flowchart TB
    subgraph ENV["☁️ 실행 환경"]
        DOCKER["🐳 Docker Container"]
        CRON["⏰ Cron Scheduler"]
    end

    subgraph INPUT["📥 입력 소스"]
        AZURE["Azure Updates<br/>(동적 웹페이지)"]
        AWS["AWS What's New<br/>(RSS Feed)"]
        GCP["GCP Release Notes<br/>(RSS Feed)"]
    end

    subgraph CRAWLER["🕷️ 크롤러 모듈"]
        AZURE_CRAWLER["Azure Crawler<br/>(Playwright)"]
        AWS_CRAWLER["AWS Crawler<br/>(feedparser)"]
        GCP_CRAWLER["GCP Crawler<br/>(feedparser)"]
    end

    subgraph PROCESS["⚙️ 데이터 처리 파이프라인"]
        PARSE["HTML/RSS 파싱<br/>(BeautifulSoup4)"]
        FILTER["날짜 필터링<br/>(주간 범위)"]
        SUMMARIZE["텍스트 요약<br/>(TextRank)"]
        TRANSLATE["자동 번역<br/>(Google Translator)"]
        CLEAN["데이터 정제<br/>(정규식)"]
    end

    subgraph OUTPUT["📤 출력 모듈"]
        CHART["차트 생성<br/>(matplotlib)"]
        JSON["JSON 저장<br/>(output/)"]
        EMAIL["HTML 이메일<br/>(SMTP)"]
    end

    subgraph EXTERNAL["🌐 외부 서비스"]
        SMTP_SERVER["SMTP Server<br/>(Gmail)"]
        TRANSLATOR_API["Google Translator API"]
    end

    CRON -->|주간 실행| DOCKER
    DOCKER -->|실행| AZURE_CRAWLER
    DOCKER -->|실행| AWS_CRAWLER
    DOCKER -->|실행| GCP_CRAWLER

    AZURE --> AZURE_CRAWLER
    AWS --> AWS_CRAWLER
    GCP --> GCP_CRAWLER

    AZURE_CRAWLER --> PARSE
    AWS_CRAWLER --> PARSE
    GCP_CRAWLER --> PARSE

    PARSE --> FILTER
    FILTER --> CLEAN
    CLEAN --> SUMMARIZE
    SUMMARIZE --> TRANSLATE

    TRANSLATE --> CHART
    TRANSLATE --> JSON
    TRANSLATE --> EMAIL

    TRANSLATE -.->|API 호출| TRANSLATOR_API
    EMAIL -.->|발송| SMTP_SERVER

    style DOCKER fill:#2196F3,color:#fff
    style AZURE_CRAWLER fill:#0078D4,color:#fff
    style AWS_CRAWLER fill:#FF9900,color:#000
    style GCP_CRAWLER fill:#4285F4,color:#fff
    style SUMMARIZE fill:#9C27B0,color:#fff
    style EMAIL fill:#4CAF50,color:#fff
```

### 2.2 코드 모듈 구조

```mermaid
graph LR
    subgraph MAIN["main.py (433줄)"]
        subgraph CONFIG["설정 모듈"]
            ENV_LOAD["환경변수 로딩<br/>load_dotenv()"]
            DATE_CALC["날짜 계산<br/>(주간 범위)"]
        end

        subgraph CRAWLERS["크롤러 함수"]
            FUNC_AZURE["crawl_azure_updates()"]
            FUNC_AWS["crawl_aws_updates()"]
            FUNC_GCP["crawl_gcp_updates()"]
        end

        subgraph NLP["자연어 처리"]
            FUNC_SUM["summarize_text()"]
            FUNC_SUM_LONG["summarize_long_text()"]
            FUNC_TRANS["translate_to_korean()"]
            FUNC_CLEAN_AWS["clean_aws_summary()"]
            FUNC_CLEAN_GCP["clean_gcp_summary()"]
        end

        subgraph VIZ["시각화"]
            FUNC_FONT["set_korean_font()"]
            FUNC_CHART["generate_summary_chart()"]
            FUNC_BANNER["get_banner_base64()"]
        end

        subgraph IO["입출력"]
            FUNC_SAVE["save_files()"]
            FUNC_EMAIL["send_email()"]
            FUNC_RETRY["send_with_retry()"]
        end

        subgraph DATA["전역 데이터"]
            ALL_DATA["all_data[]<br/>(수집 데이터)"]
        end
    end

    ENV_LOAD --> FUNC_AZURE
    ENV_LOAD --> FUNC_AWS
    ENV_LOAD --> FUNC_GCP
    DATE_CALC --> FUNC_AZURE
    DATE_CALC --> FUNC_AWS
    DATE_CALC --> FUNC_GCP

    FUNC_AZURE --> FUNC_SUM
    FUNC_AWS --> FUNC_CLEAN_AWS
    FUNC_GCP --> FUNC_CLEAN_GCP

    FUNC_CLEAN_AWS --> FUNC_SUM
    FUNC_CLEAN_GCP --> FUNC_SUM_LONG

    FUNC_SUM --> FUNC_TRANS
    FUNC_SUM_LONG --> FUNC_TRANS

    FUNC_TRANS --> ALL_DATA

    ALL_DATA --> FUNC_CHART
    ALL_DATA --> FUNC_SAVE
    ALL_DATA --> FUNC_EMAIL

    FUNC_FONT --> FUNC_CHART
    FUNC_BANNER --> FUNC_EMAIL
    FUNC_CHART --> FUNC_EMAIL
    FUNC_EMAIL --> FUNC_RETRY

    style FUNC_AZURE fill:#0078D4,color:#fff
    style FUNC_AWS fill:#FF9900,color:#000
    style FUNC_GCP fill:#4285F4,color:#fff
    style ALL_DATA fill:#FF5722,color:#fff
```

---

## 🔧 3. 주요 컴포넌트 상세

### 3.1 크롤러 모듈

#### Azure Crawler
```python
기술 스택: Playwright + BeautifulSoup4
URL: https://azure.microsoft.com/en-us/updates
특징:
  - Chromium 브라우저 자동 제어 (headless)
  - 동적 JavaScript 렌더링 처리
  - 페이지네이션 자동 탐지 및 순회
  - CSS 선택자 기반 데이터 추출
처리 방식:
  1. 페이지 로딩 및 대기
  2. 전체 페이지 수 파악
  3. 각 페이지 순회하며 업데이트 항목 추출
  4. 날짜 필터링 (주간 범위만)
  5. 다음 페이지 클릭
```

#### AWS Crawler
```python
기술 스택: feedparser
URL: https://aws.amazon.com/new/feed/
특징:
  - RSS 2.0 피드 파싱
  - 실시간 업데이트 수집
  - published_parsed로 날짜 정보 추출
  - HTML 태그 제거 및 텍스트 정제
처리 방식:
  1. RSS 피드 파싱
  2. 각 항목의 날짜 확인
  3. 주간 범위 필터링
  4. HTML 정제 (URL, 불필요 문구 제거)
  5. 요약 및 번역
```

#### GCP Crawler
```python
기술 스택: feedparser + 정규식
URL: https://cloud.google.com/feeds/gcp-release-notes.xml
특징:
  - RSS XML 파싱
  - 서비스별 분류 (Feature, Announcement)
  - 정규식 패턴 매칭으로 항목 분할
  - 보안 업데이트 자동 제외
처리 방식:
  1. Release Notes RSS 파싱
  2. 정규식으로 서비스명 추출
  3. Feature/Announcement 분류
  4. 각 서비스별 콘텐츠 분리
  5. 긴 텍스트 청크 요약
```

### 3.2 데이터 처리 파이프라인

```mermaid
sequenceDiagram
    participant Crawler as 크롤러
    participant Raw as 원본 텍스트
    participant Clean as 정제 모듈
    participant Sum as 요약 모듈
    participant Trans as 번역 모듈
    participant Store as 저장소

    Crawler->>Raw: HTML/RSS 데이터
    Raw->>Clean: clean_*_summary()
    Clean->>Clean: 정규식 처리
    Clean->>Clean: HTML 태그 제거
    Clean->>Clean: URL 제거
    Clean->>Sum: 정제된 텍스트
    Sum->>Sum: TextRank 알고리즘
    Sum->>Sum: 핵심 문장 추출
    Sum->>Trans: 영어 요약문
    Trans->>Trans: Google Translator API
    Trans->>Store: 한국어 요약문
    Store->>Store: all_data[] 배열에 추가
```

### 3.3 시각화 및 출력

```mermaid
graph TB
    subgraph DATA_SOURCE["데이터 소스"]
        ALL["all_data[]<br/>(수집된 모든 업데이트)"]
    end

    subgraph CHART_GEN["차트 생성"]
        COUNT["벤더별 카운트"]
        FONT["한글 폰트 설정<br/>(OS별 분기)"]
        PIE["파이 차트 생성<br/>(matplotlib)"]
        BASE64_CHART["Base64 인코딩"]
    end

    subgraph EMAIL_GEN["이메일 생성"]
        BANNER_LOAD["배너 이미지 로드"]
        BASE64_BANNER["Base64 인코딩"]
        HTML_BUILD["HTML 구성"]
        MIME["MIME Multipart"]
    end

    subgraph SEND["발송"]
        SMTP["SMTP 연결"]
        RETRY["재시도 로직<br/>(최대 3회)"]
    end

    subgraph FILE["파일 저장"]
        JSON_OUT["JSON 포맷"]
        FILE_WRITE["output/ 저장"]
    end

    ALL --> COUNT
    COUNT --> PIE
    FONT --> PIE
    PIE --> BASE64_CHART

    BANNER_LOAD --> BASE64_BANNER
    BASE64_CHART --> HTML_BUILD
    BASE64_BANNER --> HTML_BUILD
    ALL --> HTML_BUILD

    HTML_BUILD --> MIME
    MIME --> SMTP
    SMTP --> RETRY

    ALL --> JSON_OUT
    JSON_OUT --> FILE_WRITE

    style PIE fill:#9C27B0,color:#fff
    style SMTP fill:#4CAF50,color:#fff
    style FILE_WRITE fill:#FF9800,color:#000
```

---

## 🐳 4. Docker 컨테이너 아키텍처

```mermaid
graph TD
    subgraph HOST["호스트 시스템"]
        ENV_FILE[".env 파일<br/>(SMTP 설정)"]
        OUTPUT_DIR["output/ 디렉토리"]
        CRON_JOB["Cron Job<br/>(주간 실행)"]
    end

    subgraph DOCKER["Docker Container"]
        subgraph BASE["베이스 레이어"]
            PYTHON["Python 3.11-slim"]
        end

        subgraph SYSTEM["시스템 레이어"]
            FONTS["한글 폰트<br/>(fonts-nanum)"]
            UV["uv 패키지 관리자"]
            CURL["curl"]
        end

        subgraph APP_LAYER["애플리케이션 레이어"]
            DEPS["Python 의존성<br/>(requirements.txt)"]
            PLAYWRIGHT_BIN["Playwright Chromium"]
            MATPLOTLIB_CACHE["Matplotlib 폰트 캐시"]
        end

        subgraph RUNTIME["런타임 레이어"]
            MAIN_PY["main.py"]
            BANNER["banner.png"]
        end
    end

    subgraph EXTERNAL_SERVICES["외부 서비스"]
        AZURE_WEB["Azure Updates"]
        AWS_RSS["AWS RSS"]
        GCP_RSS["GCP RSS"]
        GMAIL["Gmail SMTP"]
        GOOGLE_API["Google Translator"]
    end

    CRON_JOB -->|실행| DOCKER
    ENV_FILE -.->|마운트| DOCKER
    OUTPUT_DIR -.->|볼륨| DOCKER

    PYTHON --> FONTS
    PYTHON --> UV
    PYTHON --> CURL
    
    UV --> DEPS
    DEPS --> PLAYWRIGHT_BIN
    PLAYWRIGHT_BIN --> MATPLOTLIB_CACHE

    MATPLOTLIB_CACHE --> MAIN_PY
    BANNER --> MAIN_PY

    MAIN_PY -->|크롤링| AZURE_WEB
    MAIN_PY -->|크롤링| AWS_RSS
    MAIN_PY -->|크롤링| GCP_RSS
    MAIN_PY -->|번역| GOOGLE_API
    MAIN_PY -->|발송| GMAIL

    MAIN_PY -->|저장| OUTPUT_DIR

    style DOCKER fill:#2196F3,color:#fff
    style UV fill:#FFC107,color:#000
    style MAIN_PY fill:#4CAF50,color:#fff
```

---

## 📦 5. 의존성 맵

```mermaid
graph LR
    subgraph CORE["핵심 라이브러리"]
        BS4["beautifulsoup4<br/>4.13.3"]
        PW["playwright<br/>1.51.0"]
        SUMY["sumy<br/>0.11.0"]
        FP["feedparser<br/>6.0.11"]
        DT["deep-translator<br/>1.11.4"]
        MPL["matplotlib<br/>3.10.1"]
        DOTENV["python-dotenv<br/>1.1.0"]
    end

    subgraph FUNCTIONS["함수별 의존성"]
        F_AZURE["crawl_azure_updates()"]
        F_AWS["crawl_aws_updates()"]
        F_GCP["crawl_gcp_updates()"]
        F_SUM["summarize_text()"]
        F_TRANS["translate_to_korean()"]
        F_CHART["generate_summary_chart()"]
    end

    subgraph STDLIB["표준 라이브러리"]
        SMTP["smtplib"]
        EMAIL["email.mime"]
        JSON["json"]
        RE["re"]
        DATETIME["datetime"]
        OS["os"]
        BASE64["base64"]
        PLATFORM["platform"]
    end

    PW --> F_AZURE
    BS4 --> F_AZURE
    FP --> F_AWS
    FP --> F_GCP
    SUMY --> F_SUM
    DT --> F_TRANS
    MPL --> F_CHART
    DOTENV --> F_AZURE

    SMTP --> F_AZURE
    EMAIL --> F_AZURE
    JSON --> F_AZURE
    RE --> F_AZURE
    DATETIME --> F_AZURE
    OS --> F_AZURE
    BASE64 --> F_CHART
    PLATFORM --> F_CHART

    style PW fill:#2E7D32,color:#fff
    style SUMY fill:#9C27B0,color:#fff
    style MPL fill:#F44336,color:#fff
```

---

## 🔄 6. 데이터 흐름 상세

### 6.1 수집 단계

```
[Azure Web] --HTTP--> [Playwright] --HTML--> [BeautifulSoup4] --Extract--> [Raw Data]
[AWS RSS]   --HTTP--> [feedparser] --Parse--> [Entry Objects] --Extract--> [Raw Data]
[GCP RSS]   --HTTP--> [feedparser] --Parse--> [Entry Objects] --Regex--> [Raw Data]
```

### 6.2 처리 단계

```
[Raw Data] --> [clean_*_summary()] --> [Cleaned Text]
           --> [날짜 필터링] --> [주간 데이터만]
           --> [summarize_text()] --> [TextRank] --> [핵심 문장 2-3개]
           --> [translate_to_korean()] --> [Google API] --> [한국어 요약]
           --> [all_data[]] (전역 배열에 저장)
```

### 6.3 출력 단계

```
[all_data[]]
  ├─> [generate_summary_chart()] --> [PNG] --> [Base64] --> [Email HTML]
  ├─> [save_files()] --> [JSON] --> [output/cloud_updates_week_YYYY-MM-DD.json]
  └─> [send_email()]
        ├─> [HTML 구성] (배너 + 차트 + 업데이트 목록)
        ├─> [MIME Multipart]
        └─> [SMTP 발송] (재시도 3회)
```

---

## ⚙️ 7. 실행 플로우

```mermaid
stateDiagram-v2
    [*] --> 환경변수로딩
    환경변수로딩 --> 날짜범위계산
    날짜범위계산 --> Azure크롤링
    Azure크롤링 --> GCP크롤링
    GCP크롤링 --> AWS크롤링
    
    AWS크롤링 --> 파일저장
    파일저장 --> 이메일발송
    
    이메일발송 --> 성공확인
    성공확인 --> [*]: 완료
    
    성공확인 --> 재시도: 실패
    재시도 --> 성공확인: 3회까지
    재시도 --> [*]: 3회 초과

    note right of Azure크롤링
        Playwright 브라우저 실행
        페이지네이션 처리
        데이터 수집 및 요약
    end note

    note right of 파일저장
        JSON 포맷으로 저장
        output/ 디렉토리
    end note

    note right of 이메일발송
        HTML 뉴스레터 생성
        차트 임베딩
        SMTP 발송
    end note
```

---

## 🔐 8. 보안 및 설정

### 환경 변수 (.env)
```bash
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER=youremail@gmail.com
EMAIL_PASSWORD=yourpassword
RECEIVERS=email1@example.com,email2@example.com
```

### Git 제외 파일 (.gitignore)
```
/app/output/
*.env
*.pyc
__pycache__/
*.log
```

---

## 📊 9. 아키텍처 특징 요약

| 계층 | 기술 | 목적 |
|------|------|------|
| **인프라** | Docker + Cron | 격리된 실행 환경, 자동 스케줄링 |
| **크롤링** | Playwright + feedparser | 동적/정적 페이지 수집 |
| **파싱** | BeautifulSoup4 + 정규식 | HTML/XML 데이터 추출 |
| **NLP** | TextRank + Google Translator | 요약 및 번역 |
| **시각화** | matplotlib | 차트 생성 |
| **통신** | SMTP + 재시도 로직 | 안정적 이메일 발송 |
| **저장** | JSON 파일 | 데이터 아카이빙 |

### 설계 원칙
✅ **모듈화**: 각 크롤러와 처리 함수가 독립적으로 동작  
✅ **확장성**: 새로운 클라우드 벤더 추가 용이  
✅ **안정성**: 재시도 로직 및 예외 처리 구현  
✅ **재현성**: Docker 컨테이너로 환경 일관성 보장  
✅ **자동화**: Cron 스케줄링으로 무인 운영  

---

**본 문서는 csp-scrapper-uv 프로젝트의 전체 아키텍처를 시각적으로 표현하고 각 컴포넌트의 역할과 상호작용을 설명합니다.**
